{% extends 'base.html.twig' %}


{% block title %}Edit Projects{% endblock %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <h1>Edition du projet</h1>
        </div>
    </div>

    <div class="container">
    {{ include('projects/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

     <div class="row">
    <a class="btn btn-link" href="{{ path('projects_index') }}">Retour à la liste</a>

    {{ include('projects/_delete_form.html.twig') }}
     </div>
    </div>
{% endblock %}
