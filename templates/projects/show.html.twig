{% extends 'base.html.twig' %}

{% block title %}Projects{% endblock %}

{% block body %}
    <h1>Projects</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>GitLabId</th>
            <th>Name</th>
        </tr>
        <tr>

            <td>{{ project.gitLabId }}</td>
            <td>{{ project.name }}</td>
        </tr>

        </tbody>
    </table>

    <a class="btn btn-link" href="{{ path('projects_index') }}">back to list</a>

    <a class="btn btn-secondary" href="{{ path('projects_edit', {'gitLabId': project.gitLabId}) }}">edit</a>

    {{ include('projects/_delete_form.html.twig') }}
{% endblock %}
