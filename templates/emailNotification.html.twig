{% block html %}
    <h1>#BalanceTonMr</h1>

    <p>test message via Twig</p>
    <p>voici la liste des projets : </p>

{% for project in projects %}
    <li>{{ project.id }}</li>
    <li>{{ project.gitLabId }}</li>
    <li>{{ project.name }}</li>
{% endfor %}

    {% for request in requests %}
    <li> {{ request.title }}</li>
{% endfor %}
{% endblock %}

