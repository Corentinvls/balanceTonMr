

{% block body %}

    {{ include('team/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-link" href="{{ path('team_index') }}">back to list</a>

    {{ include('team/_delete_form.html.twig') }}
{% endblock %}
