{% extends 'base.html.twig' %}

{% block title %}UsersToTeam{% endblock %}

{% block body %}
    <h1>UsersToTeam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ users_to_team.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('users_to_team_index') }}">back to list</a>

    <a href="{{ path('users_to_team_edit', {'id': users_to_team.id}) }}">edit</a>

    {{ include('users_to_team/_delete_form.html.twig') }}
{% endblock %}
