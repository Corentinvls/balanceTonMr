{% extends 'base.html.twig' %}

{% block title %}UsersToTeam index{% endblock %}

{% block body %}
    <h1>UsersToTeam index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for users_to_team in users_to_teams %}
            <tr>
                <td>{{ users_to_team.id }}</td>
                <td>
                    <a href="{{ path('users_to_team_show', {'id': users_to_team.id}) }}">show</a>
                    <a href="{{ path('users_to_team_edit', {'id': users_to_team.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('users_to_team_new') }}">Create new</a>
{% endblock %}
